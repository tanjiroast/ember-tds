{{page-title "Board"}}
<div class="ui green segment">
  <span class="ui label">{{@model.employee.email}}</span>
  <LinkTo @route="logout" class="ui black button">
    Deconnexion
  </LinkTo>
</div>
<h2>Orders</h2>
<ul>
  {{#each @model.orders as |order|}}
  <li>
    {{moment-format order.dateCreation "LLLL"}} <span class="ui circular label">{{order.orderdetails.length}}</span>
  </li>
{{/each}}
  <LinkTo @route="products">
    <button class="ui green button"> Produits </button>
  </LinkTo>
</ul>
{{outlet}}